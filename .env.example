# Frontend (Vite)
VITE_BACKEND_URL=/api
VITE_KEYCLOAK_URL=http://localhost:8080
VITE_KEYCLOAK_REALM=Jarvis
VITE_KEYCLOAK_CLIENT_ID=react-frontend
# Optional explicit override; defaults to ${VITE_BACKEND_URL}/keycloak
# VITE_KEYCLOAK_ADMIN_API_URL=/api/keycloak

# Keycloak Admin API (Deno server: src/keycloak/admin-api/index.ts)
# Required for /api/keycloak/* routes to work.
KEYCLOAK_BASE_URL=http://localhost:8080
KEYCLOAK_REALM=Jarvis
KEYCLOAK_ADMIN_CLIENT_ID=admin-api-client
KEYCLOAK_ADMIN_CLIENT_SECRET=replace-with-real-secret
# Optional admin-api port (defaults to 8000)
# PORT=8000
